import { PipeResolver } from "@angular/compiler";
import { faPeopleCarry } from "@fortawesome/free-solid-svg-icons";

export class Fav {
  id: number;
  name: string;
  temp: number;
  humidity: string;
  description: string;
}
